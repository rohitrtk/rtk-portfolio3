[build]
    command = "pnpm run build"
    publish = "dist"
    functions = "netlify/functions"

[dev]
    command = "pnpm run dev"
    port = 8888
    targetPort = 5173
    publish = "dist"
    functions = "netlify/functions"

[[redirects]]
    from = "/*"
    to = "/index.html"
    status = 200
